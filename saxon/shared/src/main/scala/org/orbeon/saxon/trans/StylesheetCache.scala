package org.orbeon.saxon.trans

import org.orbeon.saxon.om.NodeInfo

import java.util.Map

import java.util.concurrent.ConcurrentHashMap

class StylesheetCache {
/*

  private var cacheByText: Map[String, XsltExecutable] =
    new ConcurrentHashMap[String, XsltExecutable]()

  private var cacheByLocation: Map[String, XsltExecutable] =
    new ConcurrentHashMap[String, XsltExecutable]()

  private var cacheByNode: Map[NodeInfo, XsltExecutable] =
    new ConcurrentHashMap[NodeInfo, XsltExecutable]()

  def getStylesheetByText(style: String): XsltExecutable =
    cacheByText.get(style)

  def getStylesheetByLocation(style: String): XsltExecutable =
    cacheByLocation.get(style)

  def getStylesheetByNode(style: NodeInfo): XsltExecutable =
    cacheByNode.get(style)

  def setStylesheetByText(style: String,
                          xsltExecutable: XsltExecutable): Unit = {
    cacheByText.put(style, xsltExecutable)
  }

  def setStylesheetByLocation(style: String,
                              xsltExecutable: XsltExecutable): Unit = {
    cacheByLocation.put(style, xsltExecutable)
  }

  def setStylesheetByNode(style: NodeInfo,
                          xsltExecutable: XsltExecutable): Unit = {
    cacheByNode.put(style, xsltExecutable)
  }
*/

}
